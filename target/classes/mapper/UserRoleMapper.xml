<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.vote.dao.UserRoleMapper">

    <insert id="insertUserRole" parameterType="UserRole" useGeneratedKeys="false">
        insert into `user_role` (user_id, role_id, create_time, update_time)
        values (#{userId}, #{roleId}, #{createTime}, #{updateTime});
    </insert>
</mapper>
